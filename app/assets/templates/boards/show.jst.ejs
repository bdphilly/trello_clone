<h1>A fancy board</h1>

Board Title:
<h3><%= board.escape('title') %></h3>
<% board.lists().each(function(list) { %>
	<h4><%= list.escape('title') %>
			<%= list.escape('rank') %>
	</h4>

<% }) %>
<br><br>

<a href="#">The Boards</a>